# 2026-02-12 Memory

## Created digest-and-improve skill
- Location: `skills/digest-and-improve/SKILL.md`
- Purpose: Digest content from various sources (Twitter, calendar, email, Slack) and write proposals to RLT (Resource Learning Tree)
- Created knowledge base directories: `docs/twitter/`, `docs/calendar/`, `docs/email/`, `docs/slack/`
- RLT structure: `rlt/<category>/YYYY-MM-DD.md` (categories: tech, design, life, business)
- Skill steps:
  1. Get content (web_fetch/browser/manual)
  2. Write digest to `docs/twitter/<YYYY-MM-DD>-<slug>.md`
  3. Identify improvements
  4. Write proposal to RLT file
  5. Commit + push + notify user
- Commit: 06d1601

## Installed bird CLI (v0.8.0)
- Purpose: Twitter/X command-line tool for reading tweets, bookmarks, search, etc.
- Key commands:
  - `bookmarks` - Get bookmarked tweets
  - `read <url>` - Read a specific tweet
  - `user-tweets @username` - Get user's tweets
  - `search <query>` - Search tweets
  - `home` - Get "For You" timeline
  - `likes` - Get liked tweets
- Auth: requires `auth_token` and `ct0` cookies from x.com

## Discovered bookmark-intelligence skill
- Location: `https://github.com/openclaw/skills/blob/main/skills/bkrigmo1/bookmark-intelligence/SKILL.md`
- Purpose: Turn X bookmarks into actionable insights using AI
- Features:
  - Monitors X bookmarks automatically
  - Fetches full content from linked articles
  - AI analysis with GPT-4o-mini
  - Relates insights to your projects
  - Telegram notifications
- Pricing tiers: Free (10/month), Pro ($9/mo), Enterprise ($29/mo)
- Uses bird CLI for Twitter access

## Twitter content access issue
- web_fetch fails due to privacy protection (returns login page)
- browser requires manual tab attachment (click Chrome extension)
- bird CLI is the solution for automated Twitter access

## RLT (Resource Learning Tree) Structure
- Categories: tech, design, life, business
- Format: `rlt/<category>/YYYY-MM-DD.md`
- Proposal format:
  ```markdown
  ## <Title>
  ### 来源
  ### 改进点
  ### 建议行动
  ### 关联项目/领域
  ### 优先级
  ```

## Configured Slack integration
- Channel: #rudy-and-ruby-and-ann
- User: Rudy Lu (UR256PFEE)
- Message ID format: 1770908237.645449

## Next steps for digest-and-improve
- Need to get X cookies (auth_token, ct0) to configure bird CLI
- Once configured, can automatically fetch Twitter content
- Can then execute full digest-and-improve workflow

## Slack typing indicator troubleshooting
- Emma's new Slack app doesn't show typing indicators, but Ruby's old app does
- Both apps configured with Socket Mode (`mode: "socket"`)
- Config has two Slack accounts:
  - `default`: Ruby (botToken: xoxb-...ORqJ, appToken: xapp-1-A0ABRE5E9GT...)
  - `emma`: Emma (botToken: xoxb-...RE9G, appToken: xapp-1-A0ADX4XC0CT...)
- Log shows `typing TTL reached (2m); stopping typing indicator` - typing is working but times out
- Need to check:
  - Bot scopes in Slack API Console (chat:write, channels:history, etc.)
  - Event subscriptions (app_mention, message)
  - Whether both apps are in the same workspace
- Issue not yet resolved

## Porter bot (separate OpenClaw on Linux) - No typing indicator
- Porter is also OpenClaw but deployed on Linux Ubuntu
- No typing indicator shown when Porter replies
- Need to check Porter's:
  - OpenClaw version (`openclaw --version`)
  - Slack config (`~/.openclaw/openclaw.json .channels.slack`)
  - Logs for typing errors
- Current (Mac mini) version: 2026.2.9
