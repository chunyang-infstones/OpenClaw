# 2026-02-12 Memory

## RLT-011: 工作区模板 V2.0 改动

Rudy 和 Ann 讨论后，对工作区模板做重大改动：

**1. 移除 `scripts/` folder**
- 不需要单独的脚本目录

**2. `team/` → `knowledge/`**
- 改为按产品线/项目线组织
- 示例：`knowledge/master/dapp/`, `knowledge/master/devsuite/`
- Agent 开启对话时由员工指定加载某项 knowledge

**3. 分支管理机制 (master + 日期分支)**

**workflow:**
- 当日修改 → `skills/2026-02-12/` 或 `knowledge/2026-02-12/`
- 每天早9点 → Agent 自动总结 diff，发到 channel 让 Team Lead 决定
- Team Lead 审核结果：
  - ✅ Approved → 合并到 `master/`，push GitHub
  - ❌ Denied → 删除
  - ⏸️ Pending → 带到新一天 folder
- 新日期分支组成：`master/` + pending diff = `2026-02-13/`
- 删除老分支：`2026-02-12/`

**目录结构：**
```
skills/
├── master/
│   └── skill-name/
│       └── SKILL.md
└── 2026-02-12/
    ├── skill-name/    # 修改的 skill
    ├── new-skill/     # 新增的 skill
    └── NEW.md         # 变更说明（自动生成）
```

**4. AGENT-CONFIG.md 模板**
- 替代 BOOTSTRAP.md
- Team 填写配置信息（Identity, User, Role, Tools, Heartbeat）
- Agent 读取后写入到对应文件
- 配置完成后删除

**文件标记：**
- LOCKED - 公司统一，不可修改（AGENTS.md）
- LOCKED + CUSTOM - 统一框架 + team 扩展（SOUL.md）
- CUSTOM - team 完全自定义（IDENTITY.md, USER.md, TOOLS.md, HEARTBEAT.md）
- TEMPLATE - 一次性配置（AGENT-CONFIG.md）

**PRD 更新：**
- 版本：1.0 → 2.0
- 已 commit + push (381b131)

---

## daily_standup Skill 创建

**实现细节：**
- Cron job 每天早9点触发
- 自动总结 `skills/` 和 `knowledge/` 的 diff
- 发送审核通知到 channel
- 处理 Team Lead 的 approve/deny/pending 回复
- Git 自动合并 + push

**文件：**
- `skills/daily_standup/SKILL.md` - 完整工作流程
- `skills/daily_standup/CONFIG.json` - 配置（review_channel, team_lead_mention）

**Commit:** bea4c7e

---

## AGENTS.md 压缩

**问题：** 太长，token 耗费多，重要规则被掩埋

**改动：**
- 从 ~270 行压缩到 ~60 行
- 删除冗余解释和废话
- 保留所有核心规则，提高信息密度
- 分支管理细节移到 daily_standup skill

**Commit:** 95e2eb3

---

## OpenClaw 配置文档和管理脚本

**需求：** Rudy 需要轻松切换 model 和 API Key

**产出：**

**1. `docs/OPENCLAW-CONFIG.md`** - 配置架构说明
- 配置文件位置和结构
- `openclaw.json`, `models.json`, `auth-profiles.json` 的关系
- Model 标识规则（`<provider>/<model-id>`）
- API Key 存储方式
- 多 Agent 隔离方案

**2. `scripts/agent-model-manager.sh`** - 管理脚本

**命令：**
```bash
./scripts/agent-model-manager.sh list                    # 列出所有 Agent
./scripts/agent-model-manager.sh show <agent-id>         # 显示 Agent 配置
./scripts/agent-model-manager.sh set-model <id> <model> # 设置 primary 模型
./scripts/agent-model-manager.sh add-fallback <id> <model>    # 添加 fallback
./scripts/agent-model-manager.sh remove-fallback <id> <model> # 删除 fallback
./scripts/agent-model-manager.sh update-key <provider> <api-key>  # 更新 API Key
./scripts/agent-model-manager.sh providers               # 显示所有 providers
./scripts/agent-model-manager.sh add-provider <id> <url> <key> [model]  # 添加 provider
```

**特性：**
- 自动备份配置文件
- 修改后提示重启 gateway
- 彩色输出，易读

**Commit:** 5f72d7f

---

## digest-and-improve Skill 创建

**需求：** Rudy 要我学习外部内容并自我提升

**流程：**
1. **Step 1**: 获取内容（web_fetch/browser/手动）
2. **Step 2**: 写 digest 到 `docs/twitter/`
3. **Step 3**: 识别改进点
4. **Step 4**: 写 proposal 到对应 RLT
5. **Step 5**: Commit + push + 通知 Rudy

**文件：**
- `skills/digest-and-improve/SKILL.md` - 完整工作流程

**待处理：**
- 等待 Rudy 提供 Twitter 内容（web_fetch 失败，browser 需手动附加）

**Commit:** 06d1601

---

## 👥 人员动态

- **Ann** 加入团队，协助 Rudy train Ruby

---

## 📋 任务状态

- RLT-002 Discord 客服：进行中，等冷启动文档
- RLT-011 工作区模板：进行中，daily_standup skill 已完成
- RLT-014 语音对话：Phase 1 完成，等 Rudy 录音测试

---

## 💡 关键决策

1. 工作区模板采用分支管理，每日审核流程确保质量
2. Knowledge 按产品线组织，支持灵活加载
3. AGENT-CONFIG.md 简化首次配置流程
4. 分支管理具体实现放在 skill 中，不是 AGENTS.md
5. 使用脚本管理 model 和 API Key，提高效率
6. Twitter 等社交平台内容获取困难，需要 Rudy 手动提供
